<%= form_for(route) do |f| %>
  <% if route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :parent_route_id %>
    <%= f.number_field :parent_route_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :short_name %>
    <%= f.text_field :short_name %>
  </div>

  <div class="field">
    <%= f.label :route_path_markers %>
    <%= f.text_field :route_path_markers %>
  </div>

  <div class="field">
    <%= f.label :route %>
    <%= f.text_field :route %>
  </div>

  <div class="field">
    <%= f.label :line_id %>
    <%= f.text_field :line_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
